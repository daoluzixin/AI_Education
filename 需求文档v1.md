# 博文学堂 V1 需求文档

## 1. 文档信息
- **项目名称**：博文平台
- **文档名称**：V1需求文档
- **版本号**：V1.1
- **作者**：黄慕鑫
- **日期**：2026.5.07
- **目标上线时间**：2026.5.07

---

## 2. 项目背景

当前家长在寻找合适教师资源时，存在信息不透明、筛选效率低、匹配成本高的问题。与此同时，学生侧缺少统一的报名入驻入口，平台侧也缺少标准化的审核和咨询处理工具。

因此，需要搭建一个包含**家长端、学生端、后台管理端**的基础系统，完成从需求收集、学生报名、资质审核到推荐及售后咨询处理的业务闭环。

---

## 3. 项目目标

### 3.1 业务目标
- 建立家长需求在线提交入口
- 建立学生入驻报名入口
- 建立后台资质审核机制
- 支持平台向家长推荐 5 位匹配学生
- 建立咨询/售后处理机制

### 3.2 产品目标
- 降低家长表达需求的门槛
- 提升平台推荐效率
- 规范学生资料收集与审核流程
- 形成基础业务数据沉淀

---

## 4. 用户角色

### 4.1 家长端用户
**角色描述**：有学生匹配需求的家长。
**核心诉求**：
- 快速提交需求
- 获取推荐结果
- 对结果或服务进行咨询

### 4.2 学生端用户
**角色描述**：申请入驻平台的在校大学生。
**核心诉求**：
- 填写个人资料
- 提交入驻申请
- 查看审核状态
- 发起咨询

### 4.3 后台管理用户
**角色描述**：平台运营、审核、客服人员。
**核心诉求**：
- 查看和处理家长需求
- 审核学生报名资料
- 配置推荐结果
- 处理咨询工单

---

## 5. 产品范围

本期建设三个端：

1. **家长端**（移动端 H5）
2. **学生端**（移动端 H5）
3. **后台管理端**（PC Web）

---

## 5.1 技术栈与依赖版本

> **重要**：以下版本经过兼容性验证，各端开发人员必须严格按照指定版本引入依赖，**禁止自行升降版本**，避免依赖冲突。

### 后端依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| JDK | **17** | LTS 版本，编译与运行统一 JDK 17，pom.xml 中 `<java.version>17</java.version>` |
| Spring Boot | **3.2.5** | 父 POM 使用 `spring-boot-starter-parent:3.2.5`，内置管理 Spring Framework 6.1.x |
| MyBatis-Plus | **3.5.6** | Spring Boot 3 适配版，artifactId 为 `mybatis-plus-spring-boot3-starter` |
| MySQL Connector | **8.0.33** | 由 Spring Boot BOM 管理，无需手动指定 |
| Lombok | **1.18.30** | 由 Spring Boot BOM 管理 |
| Hutool | **5.8.25** | 工具类库（编号生成、日期处理等） |

### 前端依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| Node.js | **18.x LTS** | 构建环境 |
| Vue | **3.4.x** | 三端统一 Vue 3 |
| Vant | **4.8.x** | 家长端、学生端移动端组件库 |
| Element Plus | **2.6.x** | 后台管理端 PC 组件库 |
| Axios | **1.6.x** | HTTP 请求 |

### 基础设施

| 组件 | 版本 | 说明 |
|------|------|------|
| MySQL | **8.0+** | InnoDB 引擎，utf8mb4 字符集 |
| Maven | **3.8+** | 构建工具 |

### 版本约束规则

- **禁止使用 JDK 8**：Spring Boot 3.x 最低要求 JDK 17，使用 JDK 8 将无法编译
- **禁止使用 Spring Boot 2.x**：与 MyBatis-Plus Boot3 Starter、Knife4j Jakarta 版不兼容
- **禁止混用 javax 与 jakarta**：Spring Boot 3 已全面迁移至 jakarta 命名空间，所有注解/包名使用 `jakarta.*`
- **MyBatis-Plus 必须使用 Boot3 专用 Starter**：`mybatis-plus-spring-boot3-starter`，而非旧版 `mybatis-plus-boot-starter`
- **Knife4j 必须使用 Jakarta 版**：`knife4j-openapi3-jakarta-spring-boot-starter`，而非 `knife4j-openapi3-spring-boot-starter`
- 数据库字段 snake_case，Java 实体类字段 camelCase，由 MyBatis-Plus 自动映射（`map-underscore-to-camel-case: true`）

---

# 6. 功能需求

---

## 6.1 家长端

家长端核心用户路径：**预览平台 → 触发登录 → 提交需求 → 查看推荐结果 → 咨询售后**

---

### 6.1.1 登录/注册

**功能描述**：
家长可先预览平台功能页面，当点击需要身份权限的功能模块时，弹出登录浮层，引导家长通过手机号验证码完成登录。首次登录自动完成注册，无需单独注册流程。

**输入项**：

| 字段 | 是否必填 | 说明 |
|------|---------|------|
| 手机号 | 必填 | 11位国内手机号 |
| 短信验证码 | 必填 | 6位数字 |

**输出结果**：
- 登录成功：进入家长首页，展示个人需求列表
- 登录失败：提示具体失败原因（验证码错误/已过期/手机号格式不正确）

**业务规则**：
- 手机号格式校验：仅支持11位国内手机号
- 验证码有效期：5分钟
- 发送频率限制：同一手机号60秒内不可重复获取验证码
- 错误次数限制：连续输错验证码5次后，冷却15分钟
- 未注册手机号首次登录自动创建账户

---

### 6.1.2 提交需求

**功能描述**：
家长填写结构化的需求信息并提交给平台，系统生成唯一需求编号，进入待处理队列。

**表单字段**：

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| 学生年级/年龄段 | 必填 | 下拉选择 | 小学1-6年级/初中/高中 |
| 需求类型 | 必填 | 下拉选择 | 学科辅导/兴趣培养/竞赛指导/升学规划/其他 |
| 所在城市/区域 | 必填 | 级联选择 | 省-市-区 |
| 期望条件 | 必填 | 文本输入 | 如性别偏好、学校要求、时间要求等 |
| 预算范围 | 选填 | 区间选择 | 元/小时，提供常见区间选项 |
| 补充说明 | 选填 | 多行文本 | 限500字以内 |

**业务规则**：
- 必填项不能为空，提交时前端校验
- 提交成功后生成唯一需求编号（如 REQ202605070001）
- 提交后状态默认为「待处理」
- 同一家长可提交多条需求
- 已提交的需求不可编辑，如需修改可关闭后重新提交

**需求状态流转**：

```
待处理 → 推荐中 → 已推荐 → 已关闭
  │                           ↑
  └─────────────────────────────┘（家长主动关闭）
```

- **待处理**：家长已提交，等待后台处理
- **推荐中**：后台已开始筛选匹配学生
- **已推荐**：推荐结果已配置完成，家长可查看
- **已关闭**：家长主动关闭或后台关闭

---

### 6.1.3 查看推荐结果

**功能描述**：
家长在「我的需求」列表中点击某条已推荐的需求，可查看平台推荐的最多5位学生信息，以精美卡片形式展示。

**卡片展示内容**：

| 字段 | 说明 |
|------|------|
| 学生昵称/姓名 | 脱敏展示，如"张同学" |
| 学校/年级 | 如"xxx大学·大二" |
| 个人标签 | 展示若干标签 |
| 自我介绍 | 摘要展示，限定字数 |
| 擅长方向 | 如"高中数学/物理竞赛" |
| 认证状态 | 徽标展示：已认证/未认证 |

**业务规则**：
- 每条需求最多展示若干位推荐学生
- 推荐结果由后台人工配置
- 若需求状态为「推荐中」，展示占位提示："正在为您匹配优质学生，请耐心等待"
- 卡片支持点击查看学生详情（展示完整自我介绍等）

---

### 6.1.4 咨询/售后

**功能描述**：
家长可针对任一需求或独立发起咨询工单，由后台客服统一处理并回复。

**表单字段**：

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| 咨询类型 | 必填 | 下拉选择 | 推荐不满意/服务问题/投诉建议/其他 |
| 问题描述 | 必填 | 多行文本 | 限若干字以内 |
| 联系方式 | 必填 | 文本输入 | 默认回填注册手机号，可修改 |
| 附件 | 选填 | 文件上传 | 支持图片/PDF，最多若干个，单个不超过5MB |

**工单状态流转**：

```
待处理 → 处理中 → 已处理 → 已关闭
```

- **待处理**：工单已提交，等待客服接单
- **处理中**：客服已接单，正在处理
- **已处理**：客服已回复，等待家长确认
- **已关闭**：问题已解决或超时自动关闭

**业务规则**：
- 家长可在「我的咨询」列表查看所有工单及状态
- 家长可查看后台的回复内容
- 已关闭的工单不可追加回复，需新建工单

---

### 6.1.5 我的需求列表

**功能描述**：
家长可查看自己提交的所有需求记录，了解每条需求的处理进度。

**列表字段**：

| 字段 | 说明 |
|------|------|
| 需求编号 | 唯一标识 |
| 需求类型 | 学科辅导/兴趣培养等 |
| 提交时间 | 精确到分钟 |
| 当前状态 | 待处理/推荐中/已推荐/已关闭 |

**支持操作**：
- 查看详情及推荐结果
- 主动关闭需求

---

## 6.2 学生端

学生端核心用户路径：**登录注册 → 填写入驻资料 → 提交审核 → 查看审核结果 → 咨询**

---

### 6.2.1 登录/注册

**功能描述**：
学生通过教育邮箱 + 手机号 + 短信验证码完成登录。教育邮箱用于初步身份识别，手机号用于后续通知和联系。首次登录自动完成注册。

**输入项**：

| 字段 | 是否必填 | 说明 |
|------|---------|------|
| 教育邮箱 | 必填 | 需为 .edu.cn 后缀 |
| 手机号 | 必填 | 11位国内手机号 |
| 短信验证码 | 必填 | 6位数字，发送至手机号 |

**业务规则**：
- 教育邮箱格式校验：必须为 .edu.cn 后缀
- 手机号格式校验：11位国内手机号
- 验证码有效期：5分钟
- 发送频率限制：同一手机号60秒内不可重复获取
- 错误次数限制：连续输错5次后冷却15分钟
- 同一教育邮箱仅允许绑定一个手机号

---

### 6.2.2 入驻报名资料填写

**功能描述**：
学生填写入驻申请资料并上传相关证明材料。资料分为基础信息和资质材料两部分。

**基础信息字段**：

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| 姓名 | 必填 | 文本 | 真实姓名 |
| 性别 | 必填 | 单选 | 男/女 |
| 年龄 | 必填 | 数字 | 18-30岁 |
| 联系方式 | 必填 | 文本 | 默认回填注册手机号 |
| 所在城市 | 必填 | 级联选择 | 省-市-区 |
| 学校名称 | 必填 | 文本/搜索选择 | 支持模糊搜索 |
| 年级 | 必填 | 下拉选择 | 大一/大二/大三/大四/研一/研二/研三 |
| 个人照片 | 必填 | 图片上传 | 真人正面照，不超过2MB |
| 自我介绍 | 必填 | 多行文本 | 限300字以内 |
| 擅长方向/标签 | 必填 | 多选+自定义 | 至少选1个，最多5个 |

**资质材料字段**：

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| 学生证/身份证明 | 必填 | 图片上传 | 学生证照片，正反面 |
| 获奖证书 | 选填 | 图片上传 | 最多上传5张 |
| 成绩证明 | 选填 | 图片上传 | 成绩单截图等 |
| 其他补充材料 | 选填 | 文件上传 | 支持图片/PDF，最多3个 |

**业务规则**：
- 支持「保存草稿」：随时保存当前填写进度，下次继续编辑
- 支持「提交审核」：所有必填项校验通过后方可提交
- 提交后状态变为「待审核」
- **草稿**状态：可随时编辑
- **待审核**状态：不可修改，等待后台审核
- **审核驳回**状态：可修改被驳回项并重新提交
- **待补充材料**状态：仅可编辑需补充的部分

---

### 6.2.3 查看审核状态

**功能描述**：
学生在个人中心查看当前入驻审核进度及结果详情。

**审核状态**：

| 状态 | 说明 | 学生可执行操作 |
|------|------|--------------|
| 草稿 | 资料未提交 | 继续编辑、提交审核 |
| 待审核 | 已提交，等待后台审核 | 无（仅查看） |
| 审核通过 | 进入平台可推荐池 | 查看个人主页 |
| 审核驳回 | 未通过，展示驳回原因 | 修改资料、重新提交 |
| 待补充材料 | 需补充指定内容 | 补充材料、重新提交 |

**展示信息**：
- 当前状态标识（醒目颜色区分）
- 审核驳回时：展示具体驳回原因
- 待补充材料时：展示需补充的具体内容说明
- 审核通过时：展示"恭喜您已通过审核，进入推荐池"

---

### 6.2.4 咨询/售后

**功能描述**：
学生可对报名流程、审核结果、资料问题发起咨询工单。

**表单字段**：

| 字段 | 是否必填 | 类型 | 说明 |
|------|---------|------|------|
| 咨询类型 | 必填 | 下拉选择 | 报名问题/审核问题/资料修改/其他 |
| 问题描述 | 必填 | 多行文本 | 限500字以内 |
| 联系方式 | 必填 | 文本输入 | 默认回填注册手机号 |
| 附件 | 选填 | 文件上传 | 支持图片/PDF，最多3个，单个不超过5MB |

**业务规则**：
- 工单状态流转与家长端一致：待处理 → 处理中 → 已处理 → 已关闭
- 学生可在「我的咨询」列表查看所有工单及后台回复

---

### 6.2.5 个人中心

**功能描述**：
学生查看和管理个人信息。

**展示内容**：
- 基本信息概览（头像、姓名、学校、年级）
- 审核状态入口
- 我的咨询入口
- 修改个人资料入口（仅审核通过后可修改非核心信息）
- 退出登录

---

## 6.3 后台管理端

后台管理端核心职责：**需求处理 + 学生审核 + 推荐配置 + 工单管理**

---

### 6.3.1 管理员登录

**功能描述**：
后台管理员通过账号密码登录管理系统。

**输入项**：

| 字段 | 是否必填 | 说明 |
|------|---------|------|
| 账号 | 必填 | 管理员账号 |
| 密码 | 必填 | 管理员密码 |

**业务规则**：
- 管理员账号由超级管理员创建分配
- 连续输错密码5次锁定账号30分钟
- 支持角色权限区分（运营/审核员/客服）

---

### 6.3.2 家长需求管理

**功能描述**：
后台查看和处理家长提交的所有需求，进行推荐配置。

**列表字段**：

| 字段 | 说明 |
|------|------|
| 需求编号 | 唯一标识 |
| 家长姓名/手机号 | 提交人信息 |
| 需求类型 | 学科辅导/兴趣培养等 |
| 区域 | 省-市-区 |
| 提交时间 | 精确到分钟 |
| 当前状态 | 待处理/推荐中/已推荐/已关闭 |

**筛选条件**：
- 按状态筛选
- 按需求类型筛选
- 按区域筛选
- 按提交时间范围筛选
- 关键词搜索（需求编号/手机号）

**支持操作**：

| 操作 | 说明 |
|------|------|
| 查看详情 | 查看需求完整信息 |
| 修改状态 | 待处理→推荐中 |
| 配置推荐学生 | 从审核通过的学生池中选择最多5位 |
| 关闭需求 | 需填写关闭原因 |

---

### 6.3.3 学生报名管理

**功能描述**：
后台查看所有学生报名信息，进行资质审核。

**列表字段**：

| 字段 | 说明 |
|------|------|
| 学生姓名 | 报名学生姓名 |
| 学校 | 所在学校 |
| 年级 | 当前年级 |
| 所在城市 | 省-市-区 |
| 提交时间 | 精确到分钟 |
| 审核状态 | 待审核/审核通过/审核驳回/待补充材料 |

**筛选条件**：
- 按审核状态筛选
- 按学校筛选
- 按城市筛选
- 按提交时间范围筛选
- 关键词搜索（姓名/学校/手机号）

**支持操作**：

| 操作 | 说明 |
|------|------|
| 查看详情 | 查看学生完整资料及上传材料 |
| 审核通过 | 学生进入可推荐池 |
| 审核驳回 | 需填写驳回原因（必填） |
| 要求补充材料 | 需说明需补充的具体内容（必填） |

---

### 6.3.4 学生资质审核

**功能描述**：
审核人员对学生提交的资料逐项核验后做出审核决策。

**审核动作**：

| 动作 | 说明 |
|------|------|
| 通过 | 学生进入推荐池，可被推荐给家长 |
| 驳回 | 需填写驳回原因，学生可修改后重新提交 |
| 补充材料 | 需说明具体需补充内容，学生补充后再次提交 |

**审核记录**（每次操作自动生成，不可删除）：

| 字段 | 说明 |
|------|------|
| 审核人 | 操作的管理员账号 |
| 审核时间 | 精确到秒 |
| 审核结果 | 通过/驳回/补充材料 |
| 审核备注/驳回原因 | 文本说明 |

**业务规则**：
- 审核记录形成完整审计轨迹，不可删除或修改
- 同一学生可被多次审核（驳回后重新提交场景）
- 所有审核历史记录在详情页可查看

---

### 6.3.5 推荐管理

**功能描述**：
后台运营人员针对某条家长需求，从已审核通过的学生池中筛选最多5位学生进行推荐配置。

**操作流程**：
1. 进入某条需求的推荐配置页面
2. 从学生池中搜索/筛选合适学生
3. 选择学生加入推荐列表（最多5位）
4. 调整推荐顺序（拖拽排序）
5. 确认提交推荐结果

**学生池筛选条件**：
- 按城市筛选
- 按擅长方向/标签筛选
- 按学校筛选
- 按年级筛选
- 关键词搜索（姓名/标签）

**业务规则**：
- 仅「审核通过」状态的学生可参与推荐
- 每条需求最多配置5位学生
- 支持调整推荐顺序（拖拽排序）
- 推荐完成后，需求状态自动更新为「已推荐」
- 已推荐的结果支持后续调整（替换/增减学生）
- 同一学生可被推荐给多条需求

---

### 6.3.6 咨询工单管理

**功能描述**：
后台统一处理来自家长端和学生端的所有咨询工单。

**列表字段**：

| 字段 | 说明 |
|------|------|
| 工单编号 | 唯一标识 |
| 用户类型 | 家长/学生 |
| 用户姓名/手机号 | 提交人信息 |
| 咨询类型 | 推荐不满意/报名问题等 |
| 提交时间 | 精确到分钟 |
| 当前状态 | 待处理/处理中/已处理/已关闭 |

**筛选条件**：
- 按用户类型筛选（家长/学生）
- 按工单状态筛选
- 按咨询类型筛选
- 按提交时间范围筛选
- 关键词搜索（工单编号/手机号）

**支持操作**：

| 操作 | 说明 |
|------|------|
| 查看详情 | 查看咨询完整信息及附件 |
| 回复咨询 | 支持文字回复 + 附件（图片/文件） |
| 修改状态 | 待处理→处理中→已处理 |
| 关闭工单 | 问题已解决，关闭工单 |

**业务规则**：
- 客服接单后状态变为「处理中」
- 回复后状态变为「已处理」
- 已关闭工单不可再操作
- 支持查看同一用户的历史工单记录

---

### 6.3.7 数据概览（Dashboard）

**功能描述**：
后台首页展示平台核心运营数据概览。

**展示指标**：

| 指标 | 说明 |
|------|------|
| 今日新增需求数 | 家长端当日提交的需求总数 |
| 待处理需求数 | 当前待处理状态的需求总数 |
| 今日新增报名数 | 学生端当日提交的报名总数 |
| 待审核学生数 | 当前待审核状态的学生总数 |
| 待处理工单数 | 当前待处理状态的工单总数 |
| 累计注册家长数 | 平台家长总数 |
| 累计入驻学生数 | 审核通过的学生总数 |

---

# 7. 核心业务流程

---

## 7.1 家长需求处理流程

```
家长登录家长端
    ↓
填写并提交需求
    ↓
系统生成需求记录（状态：待处理）
    ↓
后台运营查看该需求，修改状态为「推荐中」
    ↓
后台从审核通过的学生中筛选5位
    ↓
配置推荐结果，需求状态变为「已推荐」
    ↓
家长查看推荐结果
    ↓
如有问题 → 提交咨询工单
```

---

## 7.2 学生入驻审核流程

```
学生登录学生端
    ↓
填写入驻资料（可保存草稿）
    ↓
上传资质材料
    ↓
提交审核（状态：待审核）
    ↓
后台审核人员审核资料
    ↓
┌────────────────┬────────────────┬────────────────┐
│   审核通过      │   审核驳回      │   待补充材料    │
│ 加入推荐池     │ 展示驳回原因    │ 展示补充要求    │
│               │ 学生修改后      │ 学生补充后      │
│               │ 重新提交        │ 重新提交        │
└────────────────┴────────────────┴────────────────┘
```

---

## 7.3 售后咨询流程

```
家长/学生发起咨询
    ↓
系统生成咨询工单（状态：待处理）
    ↓
后台客服查看工单，接单（状态：处理中）
    ↓
客服处理并回复（状态：已处理）
    ↓
用户查看处理结果
    ↓
问题解决 → 关闭工单（状态：已关闭）
```

---

