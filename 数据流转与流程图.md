# 博文学堂 V1 - 数据流转与流程图

## 1. 文档信息

- **关联项目**：博文平台
- **关联需求**：需求文档v1.md
- **关联库表**：数据库设计文档.md / sql/schema.sql
- **版本号**：V1.0
- **日期**：2026.5.07

---

## 2. 系统整体数据流转图

```mermaid
graph TB
    subgraph 家长端["家长端 (H5)"]
        P1["注册/登录"]
        P2["提交需求"]
        P3["查看推荐"]
        P4["提交工单"]
    end

    subgraph 学生端["学生端 (H5)"]
        S1["注册/登录"]
        S2["填写入驻资料"]
        S3["提交审核"]
        S4["查看审核状态"]
        S5["提交工单"]
    end

    subgraph 后台管理端["后台管理端 (PC Web)"]
        A1["需求管理"]
        A2["学生管理"]
        A3["资质审核"]
        A4["推荐管理"]
        A5["工单管理"]
    end

    subgraph 数据库["MySQL 数据库"]
        DB_USER["user"]
        DB_SP["student_profile"]
        DB_RR["review_record"]
        DB_DM["demand"]
        DB_RC["recommendation"]
        DB_TK["ticket"]
        DB_TR["ticket_reply"]
    end

    P1 -->|写入| DB_USER
    P2 -->|写入| DB_DM
    P3 -->|读取| DB_RC
    P3 -->|读取| DB_SP
    P4 -->|写入| DB_TK

    S1 -->|写入| DB_USER
    S2 -->|写入| DB_SP
    S3 -->|更新 review_status| DB_SP
    S4 -->|读取| DB_SP
    S5 -->|写入| DB_TK

    A1 -->|读写| DB_DM
    A2 -->|读取| DB_SP
    A3 -->|写入| DB_RR
    A3 -->|更新| DB_SP
    A4 -->|写入| DB_RC
    A4 -->|更新| DB_DM
    A5 -->|读写| DB_TK
    A5 -->|写入| DB_TR
```

---

## 3. 核心业务流程图

### 3.1 用户注册与登录流程

```mermaid
flowchart TD
    Start(["用户打开应用"]) --> SelectRole{"选择身份"}

    SelectRole -->|家长| ParentLogin["输入手机号"]
    SelectRole -->|学生| StudentLogin["输入手机号 + 教育邮箱"]
    SelectRole -->|管理员| AdminLogin["输入手机号 + 密码"]

    ParentLogin --> SendSMS1["发送验证码"]
    SendSMS1 --> VerifyCode1{"验证码校验"}
    VerifyCode1 -->|通过| CheckParent{"用户是否存在?"}
    CheckParent -->|否| CreateParent["创建用户 (role=1)"]
    CheckParent -->|是| ParentHome["进入家长首页"]
    CreateParent --> FillNickname["填写昵称"]
    FillNickname --> ParentHome

    StudentLogin --> SendSMS2["发送验证码"]
    SendSMS2 --> VerifyCode2{"验证码校验"}
    VerifyCode2 -->|通过| VerifyEmail{"教育邮箱校验 (.edu.cn)"}
    VerifyEmail -->|通过| CheckStudent{"用户是否存在?"}
    VerifyEmail -->|失败| EmailError["提示邮箱格式错误"]
    CheckStudent -->|否| CreateStudent["创建用户 (role=2)"]
    CheckStudent -->|是| StudentHome["进入学生首页"]
    CreateStudent --> StudentHome

    AdminLogin --> VerifyPwd{"密码校验"}
    VerifyPwd -->|通过| AdminHome["进入后台首页"]
    VerifyPwd -->|失败| PwdError["提示密码错误"]

    VerifyCode1 -->|失败| CodeError1["提示验证码错误"]
    VerifyCode2 -->|失败| CodeError2["提示验证码错误"]
```

---

### 3.2 家长提交需求流程

```mermaid
flowchart TD
    Start(["家长点击'发布需求'"]) --> FillForm["填写需求表单"]

    FillForm --> FormFields["孩子年级 + 需求类型 + 城市\n+ 期望条件 + 预算范围 + 补充说明"]
    FormFields --> Validate{"表单校验"}

    Validate -->|失败| ShowError["提示必填项"]
    ShowError --> FillForm
    Validate -->|通过| Submit["提交需求"]

    Submit --> GenNo["生成需求编号 (REQ前缀)"]
    GenNo --> SaveDB["写入 demand 表\nstatus = PENDING(0)"]
    SaveDB --> Notify["通知后台运营 (新需求)"]
    Notify --> ShowSuccess["提示提交成功"]
    ShowSuccess --> ViewList["跳转到'我的需求'列表"]

    ViewList --> CheckStatus{"需求状态"}
    CheckStatus -->|PENDING| WaitTag["显示'待处理'标签"]
    CheckStatus -->|MATCHING| MatchTag["显示'推荐中'标签"]
    CheckStatus -->|RECOMMENDED| ViewRecommend["可查看推荐卡片"]
    CheckStatus -->|CLOSED| ClosedTag["显示'已关闭'标签"]

    ViewRecommend --> CardList["展示推荐学生卡片\n(最多5张, 按 sort_order 排序)"]
    CardList --> ViewDetail["点击查看学生详情"]
```

---

### 3.3 学生入驻与审核流程

```mermaid
flowchart TD
    Start(["学生点击'入驻申请'"]) --> FillBasic["填写基本信息"]
    FillBasic --> BasicFields["姓名 + 性别 + 出生日期 + 城市\n+ 学校 + 年级 + 照片 + 自我介绍\n+ 擅长方向 + 标签"]

    BasicFields --> UploadDoc["上传资质材料"]
    UploadDoc --> DocFields["学生证(必填) + 获奖证书(选填)\n+ 成绩证明(选填)"]

    DocFields --> Action{"用户操作"}
    Action -->|保存草稿| SaveDraft["写入 student_profile\nreview_status = DRAFT(0)"]
    Action -->|提交审核| SubmitReview["写入 student_profile\nreview_status = PENDING_REVIEW(1)"]

    SaveDraft --> DraftTip["提示'已保存草稿'\n可继续编辑"]

    SubmitReview --> GenSTU["生成学生编号 (STU前缀)"]
    GenSTU --> NotifyAdmin["通知后台 (新审核任务)"]
    NotifyAdmin --> WaitReview["学生端显示'审核中'"]

    WaitReview --> AdminReview(["后台审核员操作"])
    AdminReview --> ReviewAction{"审核决定"}

    ReviewAction -->|通过| Approve["更新 review_status = APPROVED(2)\n写入 review_record"]
    ReviewAction -->|驳回| Reject["更新 review_status = REJECTED(3)\n填写 reject_reason\n写入 review_record"]
    ReviewAction -->|要求补充| Supplement["更新 review_status = NEED_SUPPLEMENT(4)\n填写 supplement_note\n写入 review_record"]

    Approve --> NotifyStudent1["通知学生: 审核通过"]
    Reject --> NotifyStudent2["通知学生: 审核驳回"]
    Supplement --> NotifyStudent3["通知学生: 需补充材料"]

    NotifyStudent1 --> Visible["学生资料进入可推荐池"]
    NotifyStudent2 --> ReEdit["学生可重新编辑提交"]
    NotifyStudent3 --> UploadMore["学生上传补充材料"]
    UploadMore --> ReSubmit["重新提交审核\nreview_status → PENDING_REVIEW(1)"]
    ReSubmit --> WaitReview
    ReEdit --> FillBasic
```

---

### 3.4 后台推荐匹配流程

```mermaid
flowchart TD
    Start(["运营进入'需求管理'"]) --> SelectDemand["选择一条待处理需求"]
    SelectDemand --> ViewDemand["查看需求详情\n(孩子年级/城市/类型/期望)"]

    ViewDemand --> ChangeStatus["修改需求状态 → MATCHING(1)"]
    ChangeStatus --> SearchStudent["按条件筛选学生"]

    SearchStudent --> Filters["筛选条件:\n城市匹配 + review_status=APPROVED\n+ 擅长方向包含需求类型"]
    Filters --> StudentList["展示符合条件学生列表"]

    StudentList --> SelectStudents["勾选学生 (最多5位)"]
    SelectStudents --> SetOrder["设置推荐排序"]
    SetOrder --> Confirm{"确认推荐?"}

    Confirm -->|取消| StudentList
    Confirm -->|确认| SaveRecommend["写入 recommendation 表\n(demand_id + student_id + sort_order + operator_id)"]

    SaveRecommend --> UpdateDemand["更新需求状态 → RECOMMENDED(2)"]
    UpdateDemand --> NotifyParent["通知家长: 已有推荐结果"]
    NotifyParent --> Done["推荐完成"]

    Done --> Adjust{"后续调整?"}
    Adjust -->|替换学生| RemoveOld["删除原推荐记录"]
    RemoveOld --> SelectStudents
    Adjust -->|调整排序| UpdateOrder["更新 sort_order"]
    Adjust -->|无调整| End(["结束"])
```

---

### 3.5 咨询工单处理流程

```mermaid
flowchart TD
    Start(["用户点击'咨询/售后'"]) --> SelectType["选择咨询类型"]
    SelectType --> TypeOptions["SERVICE_CONSULT / MATCH_FEEDBACK\n/ COMPLAINT / REFUND / OTHER"]

    TypeOptions --> FillTicket["填写工单内容"]
    FillTicket --> TicketFields["问题描述 + 联系电话\n+ 关联需求(可选) + 附件(可选)"]

    TicketFields --> SubmitTicket["提交工单"]
    SubmitTicket --> GenTKT["生成工单编号 (TKT前缀)"]
    GenTKT --> SaveTicket["写入 ticket 表\nstatus = PENDING(0)"]
    SaveTicket --> NotifyCS["通知客服 (新工单)"]
    NotifyCS --> UserWait["用户端显示'待处理'"]

    UserWait --> CSAction(["客服操作"])
    CSAction --> AcceptTicket["接单: status → PROCESSING(1)\n填入 handler_id"]
    AcceptTicket --> HandleTicket{"处理方式"}

    HandleTicket -->|回复用户| WriteReply["填写回复内容\n写入 ticket_reply 表"]
    WriteReply --> UpdateResolved["更新 ticket status → RESOLVED(2)"]
    UpdateResolved --> NotifyUser["通知用户: 已回复"]

    HandleTicket -->|需多次沟通| MultiReply["多次写入 ticket_reply\nstatus 保持 PROCESSING(1)"]
    MultiReply --> FinalReply["最终回复"]
    FinalReply --> UpdateResolved

    NotifyUser --> UserCheck{"用户查看回复"}
    UserCheck -->|满意| CloseTicket["客服关闭工单\nstatus → CLOSED(3)"]
    UserCheck -->|不满意| ContinueCS["用户追问 → 新工单 或 客服继续处理"]
```

---

### 3.6 学生审核状态机

```mermaid
stateDiagram-v2
    [*] --> DRAFT: 保存草稿

    DRAFT --> PENDING_REVIEW: 提交审核
    PENDING_REVIEW --> APPROVED: 审核通过
    PENDING_REVIEW --> REJECTED: 审核驳回
    PENDING_REVIEW --> NEED_SUPPLEMENT: 要求补充

    REJECTED --> DRAFT: 重新编辑
    NEED_SUPPLEMENT --> PENDING_REVIEW: 补充后重新提交

    APPROVED --> [*]

    note right of DRAFT: review_status = 0\n仅学生端可见
    note right of PENDING_REVIEW: review_status = 1\n后台审核列表可见
    note right of APPROVED: review_status = 2\n进入可推荐池
    note right of REJECTED: review_status = 3\n学生可重新编辑
    note right of NEED_SUPPLEMENT: review_status = 4\n学生补充材料后重新提交
```

---

### 3.7 需求状态机

```mermaid
stateDiagram-v2
    [*] --> PENDING: 家长提交需求

    PENDING --> MATCHING: 运营开始匹配
    MATCHING --> RECOMMENDED: 推荐完成
    PENDING --> CLOSED: 家长主动关闭
    MATCHING --> CLOSED: 运营/家长关闭
    RECOMMENDED --> CLOSED: 家长确认/关闭

    CLOSED --> [*]

    note right of PENDING: status = 0\n等待运营处理
    note right of MATCHING: status = 1\n运营筛选学生中
    note right of RECOMMENDED: status = 2\n家长可查看推荐卡片
    note right of CLOSED: status = 3\n记录 close_reason
```

---

### 3.8 工单状态机

```mermaid
stateDiagram-v2
    [*] --> PENDING: 用户提交工单

    PENDING --> PROCESSING: 客服接单
    PROCESSING --> RESOLVED: 客服回复/处理完毕
    RESOLVED --> CLOSED: 客服关闭

    PROCESSING --> CLOSED: 客服直接关闭(无效工单)

    CLOSED --> [*]

    note right of PENDING: status = 0\n等待客服接单
    note right of PROCESSING: status = 1\n客服处理中, 可多次回复
    note right of RESOLVED: status = 2\n已回复, 等待确认
    note right of CLOSED: status = 3\n工单终结
```

---

## 4. 跨端数据流转详情

### 4.1 需求-推荐-展示 全链路

```mermaid
sequenceDiagram
    participant Parent as 家长端
    participant API as 后端 API
    participant DB as 数据库
    participant Admin as 后台管理端

    Parent->>API: POST /demand (提交需求)
    API->>DB: INSERT demand (status=0)
    API-->>Parent: 返回 demandNo

    Note over Admin: 运营收到新需求通知

    Admin->>API: GET /demands?status=0 (查看待处理需求)
    API->>DB: SELECT * FROM demand WHERE status=0
    DB-->>API: 需求列表
    API-->>Admin: 返回需求列表

    Admin->>API: PUT /demand/{id}/status (改为 MATCHING)
    API->>DB: UPDATE demand SET status=1

    Admin->>API: GET /students?city=xxx&reviewStatus=2 (筛选学生)
    API->>DB: SELECT FROM student_profile WHERE city=? AND review_status=2
    DB-->>API: 学生列表
    API-->>Admin: 返回候选学生

    Admin->>API: POST /recommendation (配置推荐)
    API->>DB: INSERT recommendation (demand_id, student_id, sort_order)
    API->>DB: UPDATE demand SET status=2
    API-->>Admin: 推荐成功

    Note over Parent: 家长收到推荐通知

    Parent->>API: GET /demand/{id}/recommendations (查看推荐)
    API->>DB: SELECT r.*, sp.* FROM recommendation r JOIN student_profile sp ON r.student_id=sp.id WHERE r.demand_id=? ORDER BY sort_order
    DB-->>API: 推荐学生详情
    API-->>Parent: 返回推荐卡片列表
```

---

### 4.2 学生入驻审核 全链路

```mermaid
sequenceDiagram
    participant Student as 学生端
    participant API as 后端 API
    participant DB as 数据库
    participant Admin as 后台管理端

    Student->>API: POST /student-profile (保存草稿)
    API->>DB: INSERT student_profile (review_status=0)
    API-->>Student: 保存成功

    Student->>API: PUT /student-profile/submit (提交审核)
    API->>DB: UPDATE student_profile SET review_status=1, student_no=生成编号
    API-->>Student: 提交成功, 返回 studentNo

    Note over Admin: 后台收到审核通知

    Admin->>API: GET /students?reviewStatus=1 (查看待审核)
    API->>DB: SELECT FROM student_profile WHERE review_status=1
    DB-->>API: 待审核列表
    API-->>Admin: 返回待审核学生

    Admin->>API: PUT /student-profile/{id}/review (审核操作)

    alt 审核通过
        API->>DB: UPDATE student_profile SET review_status=2
        API->>DB: INSERT review_record (result=2)
        API-->>Admin: 操作成功
        Note over Student: 收到通过通知
    else 审核驳回
        API->>DB: UPDATE student_profile SET review_status=3, reject_reason=xxx
        API->>DB: INSERT review_record (result=3, note=xxx)
        API-->>Admin: 操作成功
        Note over Student: 收到驳回通知, 可重新编辑
    else 要求补充
        API->>DB: UPDATE student_profile SET review_status=4, supplement_note=xxx
        API->>DB: INSERT review_record (result=4, note=xxx)
        API-->>Admin: 操作成功
        Note over Student: 收到补充通知
        Student->>API: PUT /student-profile/supplement (上传补充材料)
        API->>DB: UPDATE student_profile SET supplements=xxx, review_status=1
        API-->>Student: 重新提交成功
    end
```

---

### 4.3 工单处理 全链路

```mermaid
sequenceDiagram
    participant User as 用户端 (家长/学生)
    participant API as 后端 API
    participant DB as 数据库
    participant CS as 后台客服

    User->>API: POST /ticket (提交工单)
    API->>DB: INSERT ticket (status=0, user_type=1或2)
    API-->>User: 返回 ticketNo

    Note over CS: 客服收到新工单通知

    CS->>API: GET /tickets?status=0 (查看待处理工单)
    API->>DB: SELECT FROM ticket WHERE status=0
    DB-->>API: 工单列表
    API-->>CS: 返回待处理工单

    CS->>API: PUT /ticket/{id}/accept (接单)
    API->>DB: UPDATE ticket SET status=1, handler_id=客服ID
    API-->>CS: 接单成功

    CS->>API: POST /ticket/{id}/reply (回复)
    API->>DB: INSERT ticket_reply (ticket_id, content, replier_id)
    API->>DB: UPDATE ticket SET status=2
    API-->>CS: 回复成功

    Note over User: 用户收到回复通知

    User->>API: GET /ticket/{id}/replies (查看回复)
    API->>DB: SELECT FROM ticket_reply WHERE ticket_id=? ORDER BY create_time
    DB-->>API: 回复列表
    API-->>User: 返回回复内容

    CS->>API: PUT /ticket/{id}/close (关闭工单)
    API->>DB: UPDATE ticket SET status=3
    API-->>CS: 关闭成功
```

---

## 5. 数据权限与可见性

```mermaid
graph LR
    subgraph 家长可见数据
        PD1["自己的需求列表"]
        PD2["推荐学生卡片(脱敏)"]
        PD3["自己的工单及回复"]
    end

    subgraph 学生可见数据
        SD1["自己的档案信息"]
        SD2["审核状态与驳回原因"]
        SD3["自己的工单及回复"]
    end

    subgraph 后台可见数据
        AD1["所有需求(按权限分城市)"]
        AD2["所有学生档案"]
        AD3["所有审核记录"]
        AD4["所有推荐关系"]
        AD5["所有工单及回复"]
    end

    subgraph 数据脱敏规则
        M1["家长看学生: 隐藏手机号/邮箱"]
        M2["家长看学生: 姓名显示全称"]
        M3["学生端: 看不到需求/家长信息"]
    end
```

---

## 6. 关键业务规则汇总

```mermaid
mindmap
    root["博文学堂业务规则"]
        需求管理
            每位家长可提交多条需求
            需求状态只能前进不可回退(除CLOSED)
            任何状态都可关闭
            关闭需填写原因
        推荐管理
            每条需求最多推荐5位学生
            同一学生可被推荐给多条需求
            推荐支持排序调整
            推荐完成自动更新需求状态
        审核管理
            审核记录只增不删不改
            REJECTED和NEED_SUPPLEMENT可重新提交
            草稿状态后台不可见
            通过后进入可推荐池
        工单管理
            家长和学生共用工单系统
            通过user_type区分来源
            客服接单后才能回复
            支持多次回复
```
