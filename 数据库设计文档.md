# 博文学堂 V1 - 数据库设计文档

## 1. 文档信息

- **关联项目**：博文平台
- **关联需求**：需求文档v1.md
- **建表脚本**：sql/schema.sql
- **版本号**：V1.0
- **日期**：2026.5.07

---

## 2. 表总览

共 7 张核心表：

| 序号 | 表名 | 说明 | 关联端 |
|------|------|------|--------|
| 1 | user | 用户表（家长/学生/管理员共用，role 区分） | 全平台 |
| 2 | student_profile | 学生档案表（大学生家教入驻资料） | 学生端、后台 |
| 3 | review_record | 审核记录表（不可删除，审计轨迹） | 后台 |
| 4 | demand | 家长需求表 | 家长端、后台 |
| 5 | recommendation | 推荐关系表（需求与学生多对多） | 后台、家长端(读) |
| 6 | ticket | 咨询工单表 | 全平台 |
| 7 | ticket_reply | 工单回复表（客服回复记录） | 后台、用户端(读) |

---

## 3. ER 关系

```
user (1) ──── (0..1) student_profile       [学生角色才有档案]
user (1) ──── (0..N) demand                [家长角色提交需求]
user (1) ──── (0..N) ticket                [家长/学生提交工单]

student_profile (1) ──── (0..N) review_record     [每次审核一条记录]
student_profile (1) ──── (0..N) recommendation    [可被推荐给多条需求]

demand (1) ──── (0..5) recommendation       [每条需求最多推荐5人]
demand (1) ──── (0..N) ticket               [工单可关联需求]

ticket (1) ──── (0..N) ticket_reply         [一个工单多条回复]
```

---

## 4. 表结构详细设计

### 4.1 user（用户表）

三端共用，通过 role 字段区分用户类型。

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 用户ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 手机号 | phone | VARCHAR(11) | UNIQUE, NOT NULL | 登录凭证，全平台唯一 |
| 密码 | password | VARCHAR(128) | NULL | BCrypt加密，仅管理员使用 |
| 教育邮箱 | email | VARCHAR(128) | UNIQUE, NULL | .edu.cn后缀，仅学生 |
| 昵称 | nickname | VARCHAR(64) | NULL | 家长注册时填写 |
| 头像 | avatar_url | VARCHAR(512) | NULL | 头像地址 |
| 角色 | role | TINYINT | NOT NULL | 1-家长 2-学生 3-管理员 |
| 账号状态 | status | TINYINT | NOT NULL, DEFAULT 1 | 0-禁用 1-正常 |
| 创建时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| 更新时间 | update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `uk_phone`：手机号唯一
- `uk_email`：邮箱唯一
- `idx_role_status`：角色+状态联合索引

**与需求文档对应**：
- 家长端 7.1.1 登录：phone + nickname
- 学生端 7.2.1 登录：phone + email
- 后台 7.3.1 登录：phone (作为账号) + password

---

### 4.2 student_profile（学生档案表）

大学生入驻资料，与 user 表一对一关系。

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 档案ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 用户ID | user_id | BIGINT | UNIQUE, NOT NULL | 关联 user.id |
| 学生编号 | student_no | VARCHAR(32) | UNIQUE, NOT NULL | STU前缀，提交审核时生成 |
| 真实姓名 | real_name | VARCHAR(32) | NOT NULL | 对应接口字段 realName |
| 性别 | gender | TINYINT | NOT NULL | 1-男(MALE) 2-女(FEMALE) |
| 出生日期 | birth_date | DATE | NOT NULL | 对应接口字段 birthDate，系统计算年龄 |
| 城市 | city | VARCHAR(64) | NOT NULL | 省-市-区格式 |
| 学校名称 | school_name | VARCHAR(64) | NOT NULL | 对应接口字段 schoolName |
| 年级 | grade | VARCHAR(16) | NOT NULL | FRESHMAN/SOPHOMORE/JUNIOR/SENIOR/MASTER_1/MASTER_2/MASTER_3 |
| 个人照片 | avatar | VARCHAR(512) | NOT NULL | 对应接口字段 avatar |
| 自我介绍 | introduction | VARCHAR(500) | NULL | 限300字 |
| 擅长方向 | subjects | VARCHAR(256) | NOT NULL | 逗号分隔枚举值，如 MATH,PHYSICS |
| 个人标签 | tags | VARCHAR(256) | NOT NULL | 逗号分隔，最多5个 |
| 学生证照片 | student_id_photo | VARCHAR(1024) | NULL | 正反面URL逗号分隔 |
| 获奖证书 | certificates | VARCHAR(2048) | NULL | URL逗号分隔，最多5张 |
| 成绩证明 | transcripts | VARCHAR(1024) | NULL | URL逗号分隔，最多3张 |
| 补充材料 | supplements | VARCHAR(1024) | NULL | URL逗号分隔，最多3个 |
| 审核状态 | review_status | TINYINT | NOT NULL, DEFAULT 0 | 0-DRAFT 1-PENDING_REVIEW 2-APPROVED 3-REJECTED 4-NEED_SUPPLEMENT |
| 驳回原因 | reject_reason | VARCHAR(512) | NULL | REJECTED时由审核员填写 |
| 补充说明 | supplement_note | VARCHAR(512) | NULL | NEED_SUPPLEMENT时由审核员填写 |
| 创建时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| 更新时间 | update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `uk_user_id`：用户ID唯一（一对一）
- `uk_student_no`：学生编号唯一
- `idx_review_status`：审核状态
- `idx_city_status`：城市+审核状态联合索引（推荐筛选用）
- `idx_school`：学校名称

**与需求文档对应**：
- 学生端 7.2.2 入驻资料填写：所有基础信息 + 资质材料字段
- 后台 7.3.3 学生报名管理：列表字段
- 家长端 7.1.3 推荐卡片展示（通过 6.0.8 字段映射）

---

### 4.3 review_record（审核记录表）

每次审核操作生成一条记录，形成完整审计轨迹。**只允许 INSERT，不允许 UPDATE 和 DELETE。**

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 记录ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 学生档案ID | student_id | BIGINT | NOT NULL | 关联 student_profile.id |
| 审核人ID | reviewer_id | BIGINT | NOT NULL | 关联 user.id（管理员） |
| 审核人名 | reviewer_name | VARCHAR(64) | NOT NULL | 管理员账号名 |
| 审核结果 | review_result | TINYINT | NOT NULL | 2-APPROVED 3-REJECTED 4-NEED_SUPPLEMENT |
| 审核备注 | review_note | VARCHAR(512) | NULL | 驳回原因或补充说明 |
| 审核时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | 不可修改 |

**索引**：
- `idx_student_id`：学生档案ID
- `idx_reviewer_id`：审核人ID

**与需求文档对应**：
- 后台 7.3.4 学生资质审核 → 审核记录部分

---

### 4.4 demand（家长需求表）

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 需求ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 需求编号 | demand_no | VARCHAR(32) | UNIQUE, NOT NULL | REQ前缀，如 REQ202605070001 |
| 家长用户ID | user_id | BIGINT | NOT NULL | 关联 user.id |
| 孩子年级 | child_grade | VARCHAR(16) | NOT NULL | PRIMARY_1~SENIOR_3 |
| 需求类型 | demand_type | VARCHAR(32) | NOT NULL | SUBJECT_TUTOR/INTEREST/COMPETITION/ADMISSION/OTHER |
| 城市 | city | VARCHAR(64) | NOT NULL | 省-市-区格式 |
| 期望条件 | expectations | VARCHAR(500) | NOT NULL | 限200字 |
| 预算范围 | budget | VARCHAR(32) | NULL | 50-100/100-150/150-200/200+ |
| 补充说明 | remark | VARCHAR(1000) | NULL | 限500字 |
| 需求状态 | status | TINYINT | NOT NULL, DEFAULT 0 | 0-PENDING 1-MATCHING 2-RECOMMENDED 3-CLOSED |
| 关闭原因 | close_reason | VARCHAR(256) | NULL | CLOSED时填写 |
| 创建时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| 更新时间 | update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `uk_demand_no`：需求编号唯一
- `idx_user_id`：家长用户ID
- `idx_status`：需求状态
- `idx_city_status`：城市+状态联合索引
- `idx_demand_type`：需求类型

**与需求文档对应**：
- 家长端 7.1.2 提交需求：表单字段完全对应
- 后台 7.3.2 家长需求管理：列表字段 + 筛选条件
- 6.0.3 需求状态枚举

---

### 4.5 recommendation（推荐关系表）

需求与学生的多对多关系，每条需求最多关联5位学生。

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 推荐ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 需求ID | demand_id | BIGINT | NOT NULL | 关联 demand.id |
| 学生档案ID | student_id | BIGINT | NOT NULL | 关联 student_profile.id |
| 排序序号 | sort_order | INT | NOT NULL, DEFAULT 0 | 越小越靠前，决定卡片顺序 |
| 操作人ID | operator_id | BIGINT | NOT NULL | 关联 user.id（运营） |
| 创建时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| 更新时间 | update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `uk_demand_student`：demand_id + student_id 联合唯一（防止重复推荐）
- `idx_demand_id`：需求ID
- `idx_student_id`：学生ID

**业务约束**：同一 demand_id 下最多5条记录（应用层控制）。

**与需求文档对应**：
- 后台 7.3.5 推荐管理：操作流程
- 家长端 7.1.3 查看推荐结果：通过 demand_id JOIN student_profile 查询

---

### 4.6 ticket（咨询工单表）

家长端和学生端共用，通过 user_type 区分。

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 工单ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 工单编号 | ticket_no | VARCHAR(32) | UNIQUE, NOT NULL | TKT前缀，如 TKT202605070001 |
| 提交人ID | user_id | BIGINT | NOT NULL | 关联 user.id |
| 用户类型 | user_type | TINYINT | NOT NULL | 1-家长(PARENT) 2-学生(STUDENT) |
| 关联需求ID | related_demand_id | BIGINT | NULL | 关联 demand.id（可选） |
| 咨询类型 | ticket_type | VARCHAR(32) | NOT NULL | 见需求文档 6.0.6 枚举 |
| 问题描述 | description | VARCHAR(1000) | NOT NULL | 限500字 |
| 联系电话 | contact_phone | VARCHAR(11) | NOT NULL | 可能与注册手机号不同 |
| 附件 | attachments | VARCHAR(2048) | NULL | URL逗号分隔，最多3个 |
| 工单状态 | status | TINYINT | NOT NULL, DEFAULT 0 | 0-PENDING 1-PROCESSING 2-RESOLVED 3-CLOSED |
| 处理人ID | handler_id | BIGINT | NULL | 关联 user.id（客服接单后填入） |
| 创建时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |
| 更新时间 | update_time | DATETIME | ON UPDATE CURRENT_TIMESTAMP | |

**索引**：
- `uk_ticket_no`：工单编号唯一
- `idx_user_id`：提交人ID
- `idx_status`：工单状态
- `idx_user_type_status`：用户类型+状态联合索引
- `idx_handler_id`：处理人ID

**与需求文档对应**：
- 家长端 7.1.4 咨询/售后：提交表单字段
- 学生端 7.2.4 咨询/售后：提交表单字段
- 后台 7.3.6 咨询工单管理：列表字段 + 操作

---

### 4.7 ticket_reply（工单回复表）

客服回复记录，一个工单可有多条回复。

| 字段 | 列名 | 类型 | 约束 | 说明 |
|------|------|------|------|------|
| 回复ID | id | BIGINT | PK, AUTO_INCREMENT | 主键 |
| 工单ID | ticket_id | BIGINT | NOT NULL | 关联 ticket.id |
| 回复人ID | replier_id | BIGINT | NOT NULL | 关联 user.id（客服） |
| 回复人名 | replier_name | VARCHAR(64) | NOT NULL | 客服账号名 |
| 回复内容 | content | VARCHAR(2000) | NOT NULL | |
| 附件 | attachments | VARCHAR(2048) | NULL | URL逗号分隔 |
| 回复时间 | create_time | DATETIME | DEFAULT CURRENT_TIMESTAMP | |

**索引**：
- `idx_ticket_id`：工单ID

**与需求文档对应**：
- 后台 7.3.6 回复咨询操作
- 用户端查看后台回复内容

---

## 5. 字段命名对照表（接口 ↔ 数据库）

> 接口使用驼峰命名（camelCase），数据库使用下划线命名（snake_case），ORM 自动转换。

| 接口字段名 | 数据库列名 | 所在表 |
|-----------|-----------|--------|
| phone | phone | user |
| nickname | nickname | user |
| email | email | user |
| realName | real_name | student_profile |
| birthDate | birth_date | student_profile |
| city | city | student_profile / demand |
| schoolName | school_name | student_profile |
| grade | grade | student_profile |
| avatar | avatar | student_profile |
| introduction | introduction | student_profile |
| subjects | subjects | student_profile |
| tags | tags | student_profile |
| studentIdPhoto | student_id_photo | student_profile |
| certificates | certificates | student_profile |
| transcripts | transcripts | student_profile |
| supplements | supplements | student_profile |
| reviewStatus | review_status | student_profile |
| rejectReason | reject_reason | student_profile |
| supplementNote | supplement_note | student_profile |
| studentNo / studentId | student_no | student_profile |
| demandNo / demandId | demand_no | demand |
| childGrade | child_grade | demand |
| demandType | demand_type | demand |
| expectations | expectations | demand |
| budget | budget | demand |
| remark | remark | demand |
| closeReason | close_reason | demand |
| ticketNo / ticketId | ticket_no | ticket |
| userType | user_type | ticket |
| relatedDemandId | related_demand_id | ticket |
| ticketType | ticket_type | ticket |
| description | description | ticket |
| contactPhone | contact_phone | ticket |
| attachments | attachments | ticket / ticket_reply |
| handlerId | handler_id | ticket |
| sortOrder | sort_order | recommendation |

---

## 6. 状态值映射（枚举 ↔ 数据库 TINYINT）

### 6.1 需求状态（demand.status）

| TINYINT值 | 枚举常量 | 中文 | 触发条件 |
|-----------|---------|------|---------|
| 0 | PENDING | 待处理 | 家长提交需求时 |
| 1 | MATCHING | 推荐中 | 后台运营修改状态 |
| 2 | RECOMMENDED | 已推荐 | 后台配置推荐完成 |
| 3 | CLOSED | 已关闭 | 家长主动关闭 / 后台关闭 |

### 6.2 审核状态（student_profile.review_status）

| TINYINT值 | 枚举常量 | 中文 | 触发条件 |
|-----------|---------|------|---------|
| 0 | DRAFT | 草稿 | 学生保存草稿 |
| 1 | PENDING_REVIEW | 待审核 | 学生提交审核 |
| 2 | APPROVED | 审核通过 | 后台审核通过 |
| 3 | REJECTED | 审核驳回 | 后台审核驳回 |
| 4 | NEED_SUPPLEMENT | 待补充材料 | 后台要求补充 |

### 6.3 工单状态（ticket.status）

| TINYINT值 | 枚举常量 | 中文 | 触发条件 |
|-----------|---------|------|---------|
| 0 | PENDING | 待处理 | 用户提交工单 |
| 1 | PROCESSING | 处理中 | 客服接单 |
| 2 | RESOLVED | 已处理 | 客服回复 |
| 3 | CLOSED | 已关闭 | 客服关闭工单 |

### 6.4 用户角色（user.role）

| TINYINT值 | 枚举常量 | 中文 |
|-----------|---------|------|
| 1 | PARENT | 家长 |
| 2 | STUDENT | 学生（大学生家教） |
| 3 | ADMIN | 管理员 |

### 6.5 性别（student_profile.gender）

| TINYINT值 | 枚举常量 | 中文 |
|-----------|---------|------|
| 1 | MALE | 男 |
| 2 | FEMALE | 女 |

---

## 7. 编号生成规则

| 编号类型 | 前缀 | 格式 | 示例 | 生成时机 |
|---------|------|------|------|---------|
| 需求编号 | REQ | REQ + yyyyMMdd + 4位流水号 | REQ202605070001 | 家长提交需求时 |
| 工单编号 | TKT | TKT + yyyyMMdd + 4位流水号 | TKT202605070001 | 用户提交工单时 |
| 学生编号 | STU | STU + yyyyMMdd + 4位流水号 | STU202605070001 | 学生提交审核时 |

**实现建议**：使用 Redis INCR 生成当日流水号，key 格式为 `seq:{prefix}:{yyyyMMdd}`，设置次日凌晨过期。

---

## 8. 关键查询场景与索引对应

| 查询场景 | SQL 条件 | 命中索引 |
|---------|---------|---------|
| 后台筛选待审核学生 | WHERE review_status = 1 | idx_review_status |
| 按城市筛选可推荐学生 | WHERE city = ? AND review_status = 2 | idx_city_status |
| 家长查看自己的需求 | WHERE user_id = ? | idx_user_id (demand) |
| 后台按状态筛选需求 | WHERE status = ? | idx_status (demand) |
| 后台按城市+状态筛选需求 | WHERE city = ? AND status = ? | idx_city_status (demand) |
| 查看某需求的推荐学生 | WHERE demand_id = ? ORDER BY sort_order | idx_demand_id (recommendation) |
| 后台按用户类型筛选工单 | WHERE user_type = ? AND status = ? | idx_user_type_status |
| 查看某工单的所有回复 | WHERE ticket_id = ? ORDER BY create_time | idx_ticket_id (ticket_reply) |

---

## 9. 设计说明

### 9.1 为什么用单一 user 表而非拆分？

本期业务用户量预估较小（< 1万），家长/学生/管理员的登录鉴权逻辑一致（phone + verifyCode / password），共用一张表可以简化权限校验，通过 role 字段区分即可。后续如果用户量增长到百万级，可以考虑按 role 水平拆分。

### 9.2 为什么资质材料用 VARCHAR 存 URL 而非单独建表？

V1 期间每种材料数量有上限（学生证2张、证书5张、成绩单3张、补充材料3张），总计最多13个 URL，逗号拼接后不超过 VARCHAR(2048)。这样避免多表 JOIN，简化查询。如果后续需要对单个文件做标签、状态管理，可以抽出 `attachment` 表。

### 9.3 为什么 recommendation 不用 JSON 数组存在 demand 表里？

虽然每条需求最多5位学生，但推荐关系需要：从学生维度反查被推荐了哪些需求、支持排序调整、记录操作人。独立关系表更适合这些需求，也便于后续扩展（如加推荐理由、推荐时间等字段）。

### 9.4 DRAFT 状态为什么存数据库而非仅前端本地？

草稿保存到数据库（review_status = 0），可以实现跨设备继续编辑、防止浏览器清缓存导致数据丢失。后台列表通过 `WHERE review_status >= 1` 过滤掉草稿即可。
